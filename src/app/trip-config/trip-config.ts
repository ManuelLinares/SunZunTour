export class TripConfig {
    name: string;
    mainText: string;
    mainImgUrl;
    prop: SectionConfig[];
}

export class SectionConfig {
    bigText: string;
    smallText: string;
    imgUrl: string;
}